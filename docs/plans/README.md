# 实施计划

仅保存尚未完成、需要跨多个步骤推进的工作计划。

计划建议包含：目标、非目标、前置条件、任务清单、验证方式、风险。文件名可使用 `YYYY-MM-DD-topic.md`。

完成后应将长期有效结论更新到 requirement、architecture 或 ADR，并将计划标记为完成、归档到 `archive/`，或在无保留价值时删除。

当前没有进行中的计划。

已归档：

- [`archive/cost-aware-routing-strategy.md`](archive/cost-aware-routing-strategy.md)：成本感知路由、会话亲和与故障决策策略（状态已由 in-progress 收口为 implemented）。
